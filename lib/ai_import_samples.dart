// Sample placeholder text for the "paste words for an AI prompt" flow,
// shared between import_screen.dart (My Words import) and
// teacher_unit_screen.dart (teacher library AI-word-import) — both build an
// equivalent prompt from the same two sample forms (bare word list vs.
// already-translated pairs), previously duplicated inline in each file.
const String kSampleWordsPlain = 'apple, book, water';
const String kSampleWordsWithTranslations = 'apple - olma\nbook - kitob';
const String kSampleWordsHint =
    'apple, book, water\nor one per line\nor already-translated pairs like: apple - olma';
